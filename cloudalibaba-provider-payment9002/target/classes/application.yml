server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848 #配置Nacos地址

management:
  endpoints:
    web:
      exposure:
        include: '*' #监控端点全部打开